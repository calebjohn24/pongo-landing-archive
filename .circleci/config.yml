version: 2.1

orbs:
  node: circleci/node@16.14.2

jobs:
  build:
    docker:
      - image: circleci/node:16.14.2
    steps:
      - checkout
      - run:
          name: Install Dependencies
          command: yarn
      - run:
          name: Lint
          command: yarn lint
      - run:
          name: Build
          command: yarn build
